parameters:
    # for "merge" command
    data_to_append:
        require-dev:
            phpunit/phpunit: "^8.5.12"
            symplify/monorepo-builder: '^9.0'

    # for "split" command
    directories_to_repositories:
        packages/core: 'https://github.com/LongRunning/core.git'
        packages/doctrine-orm: 'https://github.com/LongRunning/doctrine-orm.git'
        packages/sentry: 'https://github.com/LongRunning/sentry.git'
